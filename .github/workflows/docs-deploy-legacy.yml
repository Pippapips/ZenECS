# Legacy deployment workflow (alternative approach using peaceiris/actions-gh-pages)
# This workflow deploys to gh-pages branch instead of docs/ folder
# Uncomment and use if you prefer gh-pages branch approach

# name: Deploy Documentation (gh-pages branch)
# 
# on:
#   push:
#     branches:
#       - main
#     paths:
#       - 'src/**'
#       - 'Packages/com.zenecs.core/Runtime/**'
#       - 'Packages/com.zenecs.adapter.unity/Runtime/**'
#       - 'Docs_/**'
#       - 'Docs/**'
#   workflow_dispatch:
# 
# permissions:
#   contents: write
# 
# jobs:
#   deploy-docs:
#     runs-on: ubuntu-latest
#     
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#         
#       - name: Setup .NET
#         uses: actions/setup-dotnet@v4
#         with:
#           dotnet-version: '8.0.x'
#       
#       - name: Install DocFX
#         run: dotnet tool install -g docfx
#       
#       - name: Add DocFX to PATH
#         run: echo "$HOME/.dotnet/tools" >> $GITHUB_PATH
#       
#       - name: Build ZenECS.Core
#         run: |
#           cd src
#           dotnet build ZenECS.Core.csproj -c Release /p:GenerateDocumentationFile=true
#       
#       - name: Build ZenECS.Adapter.Unity
#         run: |
#           cd src
#           dotnet build ZenECS.Adapter.Unity.csproj -c Release /p:GenerateDocumentationFile=true
#       
#       - name: Build DocFX documentation
#         run: |
#           cd Docs_
#           docfx docfx.json
#       
#       - name: Deploy to GitHub Pages (gh-pages branch)
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./Docs_/_site
#           publish_branch: gh-pages

